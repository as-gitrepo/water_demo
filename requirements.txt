fastapi==0.115.0
uvicorn[standard]==0.30.6
requests==2.32.3
pydantic==2.9.2
python-dotenv==1.0.1
langchain-core==0.3.0
# langchain not needed — we only use langchain_core directly
# No extra SDK needed for any LLM provider — all use direct HTTP via requests
